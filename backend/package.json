{
  "name": "visor-ecd-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "python3 -m venv venv && ./venv/bin/pip install -r requirements.txt",
    "start": "./venv/bin/gunicorn --bind 0.0.0.0:$PORT --workers 4 --threads 2 --timeout 300 server:app"
  }
}
