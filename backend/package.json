{
  "name": "visor-ecd-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "pip install -r requirements.txt",
    "start": "gunicorn --bind 0.0.0.0:$PORT --workers 4 --threads 2 --timeout 300 server:app"
  }
}
